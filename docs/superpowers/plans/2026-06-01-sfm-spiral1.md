# SfM Spiral 1 Implementation Plan — OpenCV Baseline Pipeline

> **For human developers** implementing step-by-step with AI assistance. Each step 2-5 minutes, TDD-driven.

**Goal:** Build a runnable SIFT feature extraction → matching → visualization pipeline using OpenCV, behind custom interfaces ready for later substitution.

**Architecture:** Three abstract interfaces (`IFeatureDetector`, `IFeatureDescriptor`, `IFeatureMatcher`) isolate algorithm details. Spiral 1 implements them via OpenCV wrappers. `SiftPipeline` wires them into a single `image → keypoints → descriptors → matches` flow.

**Tech Stack:** C++17, CMake 3.16+, OpenCV (core, features2d, imgcodecs, imgproc, highgui), Catch2 (FetchContent)

---

## File Map

```
meowmory/
├── CMakeLists.txt                 # [Modify] drop Eigen3, add C++17
├── sfm/
│   ├── CMakeLists.txt             # [Create] sfm static library
│   ├── include/sfm/
│   │   ├── sfm.h                  # [Create] top-level include
│   │   └── features/
│   │       ├── feature.h          # [Create] KeyPoint, Descriptor, FeatureMatch
│   │       ├── detector.h         # [Create] IFeatureDetector
│   │       ├── descriptor.h       # [Create] IFeatureDescriptor
│   │       ├── matcher.h          # [Create] IFeatureMatcher
│   │       ├── cv/
│   │       │   ├── cv_detector.h  # [Create] OpenCV SIFT → IFeatureDetector
│   │       │   ├── cv_descriptor.h # [Create] OpenCV SIFT → IFeatureDescriptor
│   │       │   └── cv_matcher.h   # [Create] BFMatcher+ratio → IFeatureMatcher
│   │       └── sift/
│   │           └── sift_pipeline.h # [Create] wires detector+descriptor+matcher
│   ├── src/
│   │   ├── CMakeLists.txt         # [Create] sfm source file target
│   │   └── features/
│   │       ├── cv/
│   │       │   ├── cv_detector.cpp
│   │       │   ├── cv_descriptor.cpp
│   │       │   └── cv_matcher.cpp
│   │       └── sift/
│   │           └── sift_pipeline.cpp
│   ├── tests/
│   │   ├── CMakeLists.txt         # [Create] Catch2 test target
│   │   └── test_pipeline.cpp      # [Create] integration tests
│   └── app/
│       ├── CMakeLists.txt         # [Create] demo executable target
│       └── match_demo.cpp         # [Create] end-to-end visual demo
```

---

### Task 1: Data structures and abstract interfaces

Declare types and pure virtual interfaces only. No implementations yet. Verify it compiles.

**Files:**
- Create: `sfm/include/sfm/features/feature.h`
- Create: `sfm/include/sfm/features/detector.h`
- Create: `sfm/include/sfm/features/descriptor.h`
- Create: `sfm/include/sfm/features/matcher.h`
- Create: `sfm/include/sfm/sfm.h`
- Create: `sfm/CMakeLists.txt`
- Create: `sfm/src/CMakeLists.txt`
- Modify: `CMakeLists.txt`

- [ ] **Step 1: Create feature.h**

```cpp
// sfm/include/sfm/features/feature.h
#pragma once

#include <opencv2/core.hpp>
#include <array>
#include <cstdint>

namespace sfm {

struct KeyPoint {
    cv::Point2d pt;      // subpixel position
    double size = 0.0;   // scale
    double angle = 0.0;  // dominant orientation (radians)
    double response = 0.0; // DoG response
    int octave = 0;      // pyramid octave index
};

struct Descriptor {
    std::array<uint8_t, 128> data{};
    KeyPoint keypoint;
};

struct FeatureMatch {
    int query_idx = -1;   // index into query descriptors
    int train_idx = -1;   // index into train descriptors
    double distance = 0.0; // L2 distance
};

} // namespace sfm
```

- [ ] **Step 2: Create detector.h**

```cpp
// sfm/include/sfm/features/detector.h
#pragma once

#include <opencv2/core/mat.hpp>
#include <vector>
#include "sfm/features/feature.h"

namespace sfm {

class IFeatureDetector {
public:
    virtual ~IFeatureDetector() = default;
    virtual std::vector<KeyPoint> detect(const cv::Mat& image) = 0;
};

} // namespace sfm
```

- [ ] **Step 3: Create descriptor.h**

```cpp
// sfm/include/sfm/features/descriptor.h
#pragma once

#include <opencv2/core/mat.hpp>
#include <vector>
#include "sfm/features/feature.h"

namespace sfm {

class IFeatureDescriptor {
public:
    virtual ~IFeatureDescriptor() = default;
    virtual std::vector<Descriptor> compute(
        const cv::Mat& image,
        const std::vector<KeyPoint>& keypoints) = 0;
};

} // namespace sfm
```

- [ ] **Step 4: Create matcher.h**

```cpp
// sfm/include/sfm/features/matcher.h
#pragma once

#include <vector>
#include "sfm/features/feature.h"

namespace sfm {

class IFeatureMatcher {
public:
    virtual ~IFeatureMatcher() = default;
    virtual std::vector<FeatureMatch> match(
        const std::vector<Descriptor>& query,
        const std::vector<Descriptor>& train) = 0;
};

} // namespace sfm
```

- [ ] **Step 5: Create sfm.h (top-level include)**

```cpp
// sfm/include/sfm/sfm.h
#pragma once

#include "sfm/features/feature.h"
#include "sfm/features/detector.h"
#include "sfm/features/descriptor.h"
#include "sfm/features/matcher.h"
```

- [ ] **Step 6: Create sfm/CMakeLists.txt**

```cmake
# sfm/CMakeLists.txt
find_package(OpenCV REQUIRED COMPONENTS core features2d imgcodecs imgproc highgui)

add_library(sfm STATIC "")
target_include_directories(sfm PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
target_link_libraries(sfm PUBLIC
    opencv_core
    opencv_features2d
    opencv_imgproc
    opencv_imgcodecs
)
target_compile_features(sfm PUBLIC cxx_std_17)

add_subdirectory(src)
add_subdirectory(tests)
add_subdirectory(app)
```

- [ ] **Step 7: Create sfm/src/CMakeLists.txt (empty shell)**

```cmake
# sfm/src/CMakeLists.txt
target_sources(sfm PRIVATE)
```

- [ ] **Step 8: Modify root CMakeLists.txt**

```cmake
# CMakeLists.txt (root)
cmake_minimum_required(VERSION 3.16)
project(
  MeowmoryProject
  VERSION 0.0.1
  LANGUAGES CXX
)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_subdirectory(sfm)
```

Key change: drop `find_package(Eigen3)`, set C++17.

- [ ] **Step 9: Build verification**

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build
```

Expected: compiles successfully.

- [ ] **Step 10: Commit**

```bash
git add CMakeLists.txt sfm/
git commit -m "feat(sfm): add data structures and abstract interfaces

Define KeyPoint, Descriptor, FeatureMatch structs and
IFeatureDetector, IFeatureDescriptor, IFeatureMatcher interfaces.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: OpenCV detector wrapper

**Files:**
- Create: `sfm/include/sfm/features/cv/cv_detector.h`
- Create: `sfm/src/features/cv/cv_detector.cpp`
- Modify: `sfm/src/CMakeLists.txt`

- [ ] **Step 1: Create cv_detector.h**

```cpp
// sfm/include/sfm/features/cv/cv_detector.h
#pragma once

#include <opencv2/features2d.hpp>
#include <memory>
#include "sfm/features/detector.h"

namespace sfm {

class CvSiftDetector : public IFeatureDetector {
public:
    CvSiftDetector();
    ~CvSiftDetector() override;

    std::vector<KeyPoint> detect(const cv::Mat& image) override;

private:
    cv::Ptr<cv::SIFT> sift_;
};

// Conversion: OpenCV keypoint → sfm::KeyPoint
KeyPoint from_cv_keypoint(const cv::KeyPoint& cv_kp, int octave);

} // namespace sfm
```

- [ ] **Step 2: Create cv_detector.cpp**

```cpp
// sfm/src/features/cv/cv_detector.cpp
#include "sfm/features/cv/cv_detector.h"
#include <cassert>

namespace sfm {

KeyPoint from_cv_keypoint(const cv::KeyPoint& cv_kp, int octave) {
    KeyPoint kp;
    kp.pt = cv::Point2d{cv_kp.pt.x, cv_kp.pt.y};
    kp.size = cv_kp.size;
    kp.angle = cv_kp.angle;
    kp.response = cv_kp.response;
    kp.octave = octave;
    return kp;
}

CvSiftDetector::CvSiftDetector()
    : sift_(cv::SIFT::create()) {
    assert(sift_ != nullptr);
}

CvSiftDetector::~CvSiftDetector() = default;

std::vector<KeyPoint> CvSiftDetector::detect(const cv::Mat& image) {
    assert(!image.empty());

    std::vector<cv::KeyPoint> cv_keypoints;
    sift_->detect(image, cv_keypoints);

    std::vector<KeyPoint> result;
    result.reserve(cv_keypoints.size());
    for (const auto& kp : cv_keypoints) {
        result.push_back(from_cv_keypoint(kp, kp.octave));
    }
    return result;
}

} // namespace sfm
```

- [ ] **Step 3: Register source file in sfm/src/CMakeLists.txt**

```cmake
# sfm/src/CMakeLists.txt
target_sources(sfm PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_detector.cpp
)
```

- [ ] **Step 4: Build**

```bash
cmake --build build
```

Expected: compiles successfully.

- [ ] **Step 5: Commit**

```bash
git add sfm/src/CMakeLists.txt sfm/include/sfm/features/cv/ sfm/src/features/cv/
git commit -m "feat(sfm): add CvSiftDetector — OpenCV SIFT detector wrapper"
```

---

### Task 3: OpenCV descriptor wrapper

**Files:**
- Create: `sfm/include/sfm/features/cv/cv_descriptor.h`
- Create: `sfm/src/features/cv/cv_descriptor.cpp`
- Modify: `sfm/src/CMakeLists.txt`

- [ ] **Step 1: Create cv_descriptor.h**

```cpp
// sfm/include/sfm/features/cv/cv_descriptor.h
#pragma once

#include <opencv2/features2d.hpp>
#include <memory>
#include "sfm/features/descriptor.h"

namespace sfm {

class CvSiftDescriptor : public IFeatureDescriptor {
public:
    CvSiftDescriptor();
    ~CvSiftDescriptor() override;

    std::vector<Descriptor> compute(
        const cv::Mat& image,
        const std::vector<KeyPoint>& keypoints) override;

private:
    cv::Ptr<cv::SIFT> sift_;
};

// Conversion helpers
cv::KeyPoint to_cv_keypoint(const KeyPoint& kp);
Descriptor from_cv_mat(const cv::Mat& desc_mat, int row, const KeyPoint& kp);

} // namespace sfm
```

- [ ] **Step 2: Create cv_descriptor.cpp**

```cpp
// sfm/src/features/cv/cv_descriptor.cpp
#include "sfm/features/cv/cv_descriptor.h"
#include <cassert>

namespace sfm {

cv::KeyPoint to_cv_keypoint(const KeyPoint& kp) {
    cv::KeyPoint cv_kp;
    cv_kp.pt = cv::Point2f{static_cast<float>(kp.pt.x),
                            static_cast<float>(kp.pt.y)};
    cv_kp.size = static_cast<float>(kp.size);
    cv_kp.angle = static_cast<float>(kp.angle);
    cv_kp.response = static_cast<float>(kp.response);
    cv_kp.octave = kp.octave;
    return cv_kp;
}

Descriptor from_cv_mat(const cv::Mat& desc_mat, int row, const KeyPoint& kp) {
    // OpenCV SIFT descriptor: rows = N keypoints, cols = 128, type = CV_32F
    assert(desc_mat.type() == CV_32F);
    assert(desc_mat.cols == 128);
    assert(row >= 0 && row < desc_mat.rows);

    Descriptor desc;
    desc.keypoint = kp;

    const float* row_ptr = desc_mat.ptr<float>(row);
    for (int i = 0; i < 128; ++i) {
        float val = row_ptr[i];
        if (val < 0.0f) val = 0.0f;
        if (val > 255.0f) val = 255.0f;
        desc.data[i] = static_cast<uint8_t>(val);
    }
    return desc;
}

CvSiftDescriptor::CvSiftDescriptor()
    : sift_(cv::SIFT::create()) {
    assert(sift_ != nullptr);
}

CvSiftDescriptor::~CvSiftDescriptor() = default;

std::vector<Descriptor> CvSiftDescriptor::compute(
    const cv::Mat& image,
    const std::vector<KeyPoint>& keypoints) {

    assert(!image.empty());

    // Convert keypoints to OpenCV format
    std::vector<cv::KeyPoint> cv_keypoints;
    cv_keypoints.reserve(keypoints.size());
    for (const auto& kp : keypoints) {
        cv_keypoints.push_back(to_cv_keypoint(kp));
    }

    // OpenCV SIFT compute
    cv::Mat desc_mat;
    sift_->compute(image, cv_keypoints, desc_mat);

    // Convert back to sfm::Descriptor
    std::vector<Descriptor> result;
    result.reserve(desc_mat.rows);
    for (int i = 0; i < desc_mat.rows; ++i) {
        result.push_back(from_cv_mat(desc_mat, i, keypoints[i]));
    }
    return result;
}

} // namespace sfm
```

- [ ] **Step 3: Register source file**

```cmake
# sfm/src/CMakeLists.txt
target_sources(sfm PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_detector.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_descriptor.cpp
)
```

- [ ] **Step 4: Build**

```bash
cmake --build build
```

- [ ] **Step 5: Commit**

```bash
git add sfm/ && git commit -m "feat(sfm): add CvSiftDescriptor — OpenCV SIFT descriptor wrapper"
```

---

### Task 4: OpenCV matcher wrapper

BFMatcher + Lowe's ratio test: for each query descriptor find the two nearest train descriptors, keep match only if `d1 / d2 < ratio`.

**Files:**
- Create: `sfm/include/sfm/features/cv/cv_matcher.h`
- Create: `sfm/src/features/cv/cv_matcher.cpp`
- Modify: `sfm/src/CMakeLists.txt`

- [ ] **Step 1: Create cv_matcher.h**

```cpp
// sfm/include/sfm/features/cv/cv_matcher.h
#pragma once

#include "sfm/features/matcher.h"

namespace sfm {

class CvBFMatcher : public IFeatureMatcher {
public:
    explicit CvBFMatcher(float ratio = 0.75f);

    std::vector<FeatureMatch> match(
        const std::vector<Descriptor>& query,
        const std::vector<Descriptor>& train) override;

private:
    float ratio_;
};

} // namespace sfm
```

- [ ] **Step 2: Create cv_matcher.cpp**

```cpp
// sfm/src/features/cv/cv_matcher.cpp
#include "sfm/features/cv/cv_matcher.h"
#include <opencv2/core.hpp>
#include <opencv2/features2d.hpp>
#include <cassert>

namespace sfm {

CvBFMatcher::CvBFMatcher(float ratio) : ratio_(ratio) {
    assert(ratio > 0.0f && ratio < 1.0f);
}

std::vector<FeatureMatch> CvBFMatcher::match(
    const std::vector<Descriptor>& query,
    const std::vector<Descriptor>& train) {

    if (query.empty() || train.empty()) return {};

    // Convert to cv::Mat: N x 128, CV_32F (required by BFMatcher with L2)
    auto to_cv_mat = [](const std::vector<Descriptor>& descs) -> cv::Mat {
        cv::Mat mat(static_cast<int>(descs.size()), 128, CV_32F);
        for (size_t i = 0; i < descs.size(); ++i) {
            float* row = mat.ptr<float>(static_cast<int>(i));
            for (int j = 0; j < 128; ++j) {
                row[j] = static_cast<float>(descs[i].data[j]);
            }
        }
        return mat;
    };

    cv::Mat query_mat = to_cv_mat(query);
    cv::Mat train_mat = to_cv_mat(train);

    // knnMatch with k=2 for ratio test
    cv::BFMatcher bf(cv::NORM_L2);
    std::vector<std::vector<cv::DMatch>> knn_matches;
    bf.knnMatch(query_mat, train_mat, knn_matches, 2);

    // Ratio test
    std::vector<FeatureMatch> result;
    for (const auto& knn : knn_matches) {
        if (knn.size() < 2) continue;
        if (knn[0].distance < ratio_ * knn[1].distance) {
            FeatureMatch fm;
            fm.query_idx = knn[0].queryIdx;
            fm.train_idx = knn[0].trainIdx;
            fm.distance = knn[0].distance;
            result.push_back(fm);
        }
    }
    return result;
}

} // namespace sfm
```

- [ ] **Step 3: Register source file**

```cmake
# sfm/src/CMakeLists.txt
target_sources(sfm PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_detector.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_descriptor.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_matcher.cpp
)
```

- [ ] **Step 4: Build**

```bash
cmake --build build
```

- [ ] **Step 5: Commit**

```bash
git add sfm/ && git commit -m "feat(sfm): add CvBFMatcher — BFMatcher + ratio test wrapper"
```

---

### Task 5: SiftPipeline orchestrator

Holds references to `IFeatureDetector`, `IFeatureDescriptor`, `IFeatureMatcher` (non-owning). Wires the three steps together.

**Files:**
- Create: `sfm/include/sfm/features/sift/sift_pipeline.h`
- Create: `sfm/src/features/sift/sift_pipeline.cpp`
- Modify: `sfm/src/CMakeLists.txt`

- [ ] **Step 1: Create sift_pipeline.h**

```cpp
// sfm/include/sfm/features/sift/sift_pipeline.h
#pragma once

#include <opencv2/core/mat.hpp>
#include <vector>
#include "sfm/features/feature.h"

namespace sfm {

class IFeatureDetector;
class IFeatureDescriptor;
class IFeatureMatcher;

struct MatchResult {
    std::vector<KeyPoint> query_keypoints;
    std::vector<KeyPoint> train_keypoints;
    std::vector<Descriptor> query_descriptors;
    std::vector<Descriptor> train_descriptors;
    std::vector<FeatureMatch> matches;
};

class SiftPipeline {
public:
    // Non-owning references; caller ensures lifetime
    SiftPipeline(IFeatureDetector& detector,
                 IFeatureDescriptor& descriptor,
                 IFeatureMatcher& matcher);

    MatchResult process(const cv::Mat& image1, const cv::Mat& image2);

private:
    IFeatureDetector& detector_;
    IFeatureDescriptor& descriptor_;
    IFeatureMatcher& matcher_;
};

} // namespace sfm
```

- [ ] **Step 2: Create sift_pipeline.cpp**

```cpp
// sfm/src/features/sift/sift_pipeline.cpp
#include "sfm/features/sift/sift_pipeline.h"
#include "sfm/features/detector.h"
#include "sfm/features/descriptor.h"
#include "sfm/features/matcher.h"
#include <cassert>

namespace sfm {

SiftPipeline::SiftPipeline(IFeatureDetector& detector,
                           IFeatureDescriptor& descriptor,
                           IFeatureMatcher& matcher)
    : detector_(detector)
    , descriptor_(descriptor)
    , matcher_(matcher) {}

MatchResult SiftPipeline::process(const cv::Mat& image1,
                                  const cv::Mat& image2) {
    assert(!image1.empty() && !image2.empty());

    MatchResult result;

    // 1. Detect
    result.query_keypoints = detector_.detect(image1);
    result.train_keypoints = detector_.detect(image2);

    // 2. Describe
    result.query_descriptors = descriptor_.compute(image1, result.query_keypoints);
    result.train_descriptors = descriptor_.compute(image2, result.train_keypoints);

    // 3. Match
    result.matches = matcher_.match(result.query_descriptors,
                                    result.train_descriptors);

    return result;
}

} // namespace sfm
```

- [ ] **Step 3: Register source file**

```cmake
# sfm/src/CMakeLists.txt
target_sources(sfm PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_detector.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_descriptor.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/features/cv/cv_matcher.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/features/sift/sift_pipeline.cpp
)
```

- [ ] **Step 4: Build**

```bash
cmake --build build
```

- [ ] **Step 5: Commit**

```bash
git add sfm/ && git commit -m "feat(sfm): add SiftPipeline — wires detector + descriptor + matcher"
```

---

### Task 6: match_demo — end-to-end visual demo

**Files:**
- Create: `sfm/app/CMakeLists.txt`
- Create: `sfm/app/match_demo.cpp`

- [ ] **Step 1: Create sfm/app/CMakeLists.txt**

```cmake
# sfm/app/CMakeLists.txt
add_executable(match_demo match_demo.cpp)
target_link_libraries(match_demo PRIVATE sfm opencv_highgui opencv_imgcodecs opencv_imgproc)
target_compile_features(match_demo PRIVATE cxx_std_17)
```

- [ ] **Step 2: Create match_demo.cpp**

```cpp
// sfm/app/match_demo.cpp
#include <opencv2/core.hpp>
#include <opencv2/highgui.hpp>
#include <opencv2/imgproc.hpp>
#include <iostream>
#include <cstdlib>
#include "sfm/sfm.h"
#include "sfm/features/cv/cv_detector.h"
#include "sfm/features/cv/cv_descriptor.h"
#include "sfm/features/cv/cv_matcher.h"
#include "sfm/features/sift/sift_pipeline.h"

int main(int argc, char* argv[]) {
    if (argc != 3) {
        std::cerr << "Usage: match_demo <image1> <image2>\n";
        return 1;
    }

    cv::Mat img1 = cv::imread(argv[1], cv::IMREAD_GRAYSCALE);
    cv::Mat img2 = cv::imread(argv[2], cv::IMREAD_GRAYSCALE);
    if (img1.empty() || img2.empty()) {
        std::cerr << "Failed to load images\n";
        return 1;
    }

    sfm::CvSiftDetector detector;
    sfm::CvSiftDescriptor descriptor;
    sfm::CvBFMatcher matcher(0.75f);
    sfm::SiftPipeline pipeline(detector, descriptor, matcher);

    auto result = pipeline.process(img1, img2);

    std::cout << "Keypoints image 1: " << result.query_keypoints.size() << "\n"
              << "Keypoints image 2: " << result.train_keypoints.size() << "\n"
              << "Matches: " << result.matches.size() << "\n";

    // Draw matches
    cv::Mat img1_color, img2_color;
    cv::cvtColor(img1, img1_color, cv::COLOR_GRAY2BGR);
    cv::cvtColor(img2, img2_color, cv::COLOR_GRAY2BGR);

    std::vector<cv::DMatch> cv_matches;
    for (const auto& m : result.matches) {
        cv_matches.emplace_back(m.query_idx, m.train_idx, m.distance);
    }

    std::vector<cv::KeyPoint> cv_kp1, cv_kp2;
    for (const auto& kp : result.query_keypoints) {
        cv_kp1.emplace_back(cv::Point2f{(float)kp.pt.x, (float)kp.pt.y}, (float)kp.size);
    }
    for (const auto& kp : result.train_keypoints) {
        cv_kp2.emplace_back(cv::Point2f{(float)kp.pt.x, (float)kp.pt.y}, (float)kp.size);
    }

    cv::Mat match_img;
    cv::drawMatches(img1_color, cv_kp1, img2_color, cv_kp2, cv_matches, match_img);
    cv::namedWindow("SIFT Matches", cv::WINDOW_NORMAL);
    cv::imshow("SIFT Matches", match_img);
    cv::waitKey(0);

    return 0;
}
```

- [ ] **Step 3: Build**

```bash
cmake --build build
```

- [ ] **Step 4: Run manually (need two cat images)**

```bash
./build/sfm/app/match_demo cat1.jpg cat2.jpg
```

Expected: window opens with match lines drawn. Terminal prints keypoint counts and match count.

- [ ] **Step 5: Commit**

```bash
git add sfm/app/ && git commit -m "feat(sfm): add match_demo — end-to-end SIFT matching visualizer"
```

---

### Task 7: Catch2 test framework + integration tests

**Files:**
- Create: `sfm/tests/CMakeLists.txt`
- Create: `sfm/tests/test_pipeline.cpp`

- [ ] **Step 1: Create sfm/tests/CMakeLists.txt**

```cmake
# sfm/tests/CMakeLists.txt
include(FetchContent)
FetchContent_Declare(
    Catch2
    GIT_REPOSITORY https://github.com/catchorg/Catch2.git
    GIT_TAG v3.7.1
)
FetchContent_MakeAvailable(Catch2)

add_executable(sfm_tests
    test_pipeline.cpp
)
target_link_libraries(sfm_tests PRIVATE sfm Catch2::Catch2WithMain opencv_highgui)
target_compile_features(sfm_tests PRIVATE cxx_std_17)

include(Catch)
catch_discover_tests(sfm_tests)
```

- [ ] **Step 2: Create test_pipeline.cpp**

```cpp
// sfm/tests/test_pipeline.cpp
#include <catch2/catch_test_macros.hpp>
#include <opencv2/core.hpp>
#include <opencv2/imgproc.hpp>
#include <random>
#include "sfm/sfm.h"
#include "sfm/features/cv/cv_detector.h"
#include "sfm/features/cv/cv_descriptor.h"
#include "sfm/features/cv/cv_matcher.h"
#include "sfm/features/sift/sift_pipeline.h"

namespace {

cv::Mat random_grayscale(int w, int h) {
    cv::Mat img(h, w, CV_8UC1);
    std::mt19937 rng(42);
    std::uniform_int_distribution<int> dist(0, 255);
    for (int y = 0; y < h; ++y)
        for (int x = 0; x < w; ++x)
            img.at<uint8_t>(y, x) = static_cast<uint8_t>(dist(rng));
    return img;
}

} // namespace

TEST_CASE("CvSiftDetector produces keypoints on checkerboard", "[detector]") {
    sfm::CvSiftDetector detector;

    // Use a simple pattern image instead of pure noise for more reliable detection
    cv::Mat img(256, 256, CV_8UC1, cv::Scalar(128));
    cv::rectangle(img, cv::Rect(64, 64, 128, 128), cv::Scalar(255), cv::FILLED);
    cv::GaussianBlur(img, img, cv::Size(3, 3), 1.0);

    auto kps = detector.detect(img);

    CHECK(kps.size() > 0);
    for (const auto& kp : kps) {
        CHECK(kp.pt.x >= 0.0);
        CHECK(kp.pt.y >= 0.0);
        CHECK(kp.size > 0.0);
        CHECK(kp.response > 0.0);
        CHECK(kp.octave >= 0);
    }
}

TEST_CASE("CvSiftDescriptor produces 128D descriptors per keypoint", "[descriptor]") {
    sfm::CvSiftDetector detector;
    sfm::CvSiftDescriptor descriptor;

    cv::Mat img(256, 256, CV_8UC1, cv::Scalar(128));
    cv::rectangle(img, cv::Rect(64, 64, 128, 128), cv::Scalar(255), cv::FILLED);
    cv::GaussianBlur(img, img, cv::Size(3, 3), 1.0);

    auto kps = detector.detect(img);
    REQUIRE(!kps.empty());

    auto descs = descriptor.compute(img, kps);
    REQUIRE(descs.size() == kps.size());

    for (const auto& d : descs) {
        for (uint8_t v : d.data) {
            CHECK(v <= 255); // uint8 always in range
        }
        break; // Check first descriptor only, don't need to check all 128 bytes of all
    }
}

TEST_CASE("CvBFMatcher with ratio test on identical images", "[matcher]") {
    sfm::CvSiftDetector detector;
    sfm::CvSiftDescriptor descriptor;
    sfm::CvBFMatcher matcher(0.75f);

    cv::Mat img(256, 256, CV_8UC1, cv::Scalar(128));
    cv::rectangle(img, cv::Rect(64, 64, 128, 128), cv::Scalar(255), cv::FILLED);

    // Same image twice → should have strong matches
    auto kp1 = detector.detect(img);
    auto kp2 = detector.detect(img);
    auto desc1 = descriptor.compute(img, kp1);
    auto desc2 = descriptor.compute(img, kp2);

    auto matches = matcher.match(desc1, desc2);

    CHECK(matches.size() > 0);
    for (const auto& m : matches) {
        CHECK(m.query_idx >= 0);
        CHECK(m.train_idx >= 0);
        CHECK(m.distance >= 0.0);
    }
}

TEST_CASE("SiftPipeline end-to-end produces consistent output", "[pipeline]") {
    sfm::CvSiftDetector detector;
    sfm::CvSiftDescriptor descriptor;
    sfm::CvBFMatcher matcher(0.75f);
    sfm::SiftPipeline pipeline(detector, descriptor, matcher);

    cv::Mat img1(256, 256, CV_8UC1, cv::Scalar(128));
    cv::rectangle(img1, cv::Rect(64, 64, 128, 128), cv::Scalar(255), cv::FILLED);
    cv::GaussianBlur(img1, img1, cv::Size(3, 3), 1.0);

    cv::Mat img2(256, 256, CV_8UC1, cv::Scalar(128));
    cv::rectangle(img2, cv::Rect(72, 72, 128, 128), cv::Scalar(255), cv::FILLED);
    cv::GaussianBlur(img2, img2, cv::Size(3, 3), 1.0);

    auto result = pipeline.process(img1, img2);

    CHECK(result.query_keypoints.size() > 0);
    CHECK(result.train_keypoints.size() > 0);
    CHECK(result.query_descriptors.size() == result.query_keypoints.size());
    CHECK(result.train_descriptors.size() == result.train_keypoints.size());
    // Matches may be zero for very different images, but data structures must be consistent
}
```

- [ ] **Step 3: Build and run tests**

```bash
cmake -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build
cd build && ctest --output-on-failure
```

Expected: all 4 tests PASS.

- [ ] **Step 4: Commit**

```bash
git add sfm/tests/ && git commit -m "test(sfm): add Catch2 integration tests for Spiral 1 pipeline"
```

---

## Spiral 1 Completion Checklist

- [x] Abstract interfaces compile
- [x] CvSiftDetector wraps OpenCV SIFT detect
- [x] CvSiftDescriptor wraps OpenCV SIFT compute
- [x] CvBFMatcher wraps BFMatcher + ratio test
- [x] SiftPipeline orchestrates all three
- [x] match_demo visualizes matches end-to-end
- [x] Catch2 tests pass

**Completed** — all 7 tasks merged.

---

## What's Next: Spiral 2 & 3

Spiral 1 established the baseline pipeline with OpenCV implementations behind
our interfaces. Spiral 2 and 3 replace those implementations with custom SIFT
code, one component at a time.

### Spiral 2: Replace Detector (custom DoG + keypoint localization)

| Component | Details |
|-----------|---------|
| Gaussian Pyramid | Octave construction, downsampling, σ-per-level progression |
| Difference of Gaussians | Adjacent scale subtraction across each octave |
| Keypoint Localization | 3×3×3 extrema detection, subpixel refinement (Taylor expansion), edge suppression (Hessian ratio test) |
| Benchmark | Repeatability rate and position deviation vs OpenCV SIFT |

New files (planned):
```
sfm/include/sfm/features/sift/sift_detector.h   # Implements IFeatureDetector
sfm/src/features/sift/sift_detector.cpp
sfm/tests/test_sift_detector.cpp
```

### Spiral 3: Replace Descriptor (custom gradient histogram)

| Component | Details |
|-----------|---------|
| Orientation Assignment | Gradient magnitude + orientation, 36-bin histogram, peak detection |
| Descriptor Construction | 16×16 patch → 4×4 grid × 8 orientation bins → 128D |
| Normalization | L2 normalize → clamp at 0.2 → L2 normalize |
| Benchmark | Descriptor L2 distance distribution and matching score vs OpenCV SIFT |

New files (planned):
```
sfm/include/sfm/features/sift/sift_descriptor.h  # Implements IFeatureDescriptor
sfm/src/features/sift/sift_descriptor.cpp
sfm/tests/test_sift_descriptor.cpp
```

Each spiral swaps exactly one component in the pipeline, then benchmarks
against the OpenCV baseline to validate correctness.
